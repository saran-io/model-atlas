# Week 01 — X / Twitter

Single post (under 280 chars if you shorten URLs with x.com or a link shortener). Replace `[BLOG_URL]`.

---

**Option A — single post (~270 chars with t.co links):**

Week 1/24: I'm Saran — built GPT-2 from scratch in PyTorch + real weights. Every layer explicit.

Why bother? GPT-4 / Claude / Llama are still this architecture, scaled.

Repo + explainer + blog:
https://github.com/saran-io/model-atlas
[BLOG_URL]

Co-founder, Tekvo — agentic systems daily.

---

**Option B — shorter hook + reply thread starter:**

Shipped week 1: GPT-2 from scratch + real weights. Same bones as every frontier LLM.

Code & animation: https://github.com/saran-io/model-atlas

Blog: [BLOG_URL]

(Thread 👇 on attention scaling & weight tying)

---

**Reply 1 (optional):**

Self-attention = learned lookup. QK^T / sqrt(d) isn’t cosmetic — without it, softmax saturates and you get junk even with correct checkpoints.

---

**Reply 2 (optional):**

Weight tying: the token embedding matrix *is* the output projection (transposed). Understanding “cat” and predicting “cat” share one representation.

---

Hashtags (optional on X): #LLM #PyTorch #buildinpublic
